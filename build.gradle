plugins {
    id "felis-dam" version "1.8.0-alpha"
}

group = 'org.example'
version = '1.0-SNAPSHOT'

loaderMake {
    version = "1.20.5"
}

dependencies {
    implementation("felis:felis:1.3.0-alpha")
    implementation("felis:kittens:1.2.5-alpha")
    implementation("felis:micromixin:1.2.5-alpha")
}

processResources {
    filesMatching("mods.toml") {
        expand "version": project.version
    }
}
